import type { Server as McpSdkServer } from '@modelcontextprotocol/sdk/server/index.js';
import type { McpServerOptions } from '../server.js';

export function registerResourceHandlers(_server: McpSdkServer, _opts: McpServerOptions): void {
  // Stub – implementations follow in later tasks
}
